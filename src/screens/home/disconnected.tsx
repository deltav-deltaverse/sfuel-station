import { SectionContainer } from "../../components";

const Disconnected = () => {
    return (
        <SectionContainer backgroundColor="red">
            <p>Disconnected</p>
        </SectionContainer>
    );
}

export {
    Disconnected
}